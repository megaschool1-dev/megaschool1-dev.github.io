@inherits Components.ComponentBase

@if(!Loaded)
{
    <MudSkeleton SkeletonType="SkeletonType.Rectangle" Height="@Height" />
}
else
{
    @ChildContent
}

@code {
    [Parameter]
    public bool Loaded { get; set; }

    [Parameter]
    public string? Height { get; set; } = "100px";
}